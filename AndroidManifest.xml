<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nextinpact"
    android:versionCode="10"
    android:versionName="1.7.0" >

    <!-- versionCode est utilisé par Google pour les MàJ, versionName est uniquement affiché à l'utilisateur -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />
    <!--
    minSdkVersion est la version minimum pour faire tourner l'application (2.2)
    targetSdkVersion est la version la plus élevée testée et fonctionnelle (pas besoind e faire tourner la rétro-compatibilité)
    -->

    <application
        android:icon="@drawable/logo_nextinpact"
        android:label="@string/app_name" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Light.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WebActivity"
            android:screenOrientation="sensor"
            android:theme="@android:style/Theme.Light.NoTitleBar" />
        <activity
            android:name=".CommentActivity"
            android:screenOrientation="sensor"
            android:theme="@android:style/Theme.Light.NoTitleBar" />
        <activity
            android:name=".PullToRefreshActivity"
            android:screenOrientation="sensor"
            android:theme="@android:style/Theme.Light.NoTitleBar" />
    </application>

</manifest>